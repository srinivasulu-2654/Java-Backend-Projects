server:
 port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/srinivasulu-2654/Java-Backend-Projects
          default-label: main
          clone-on-start: true 
          search-paths: config server