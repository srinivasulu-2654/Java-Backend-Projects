spring.application.name=006-eureka-server

#eureka default server 
server.port=8761

# we are using these becuase of not registering itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false